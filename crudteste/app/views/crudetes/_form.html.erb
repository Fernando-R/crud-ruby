<%= form_with(model: crudete, local: true) do |form| %>
  <% if crudete.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crudete.errors.count, "error") %> prohibited this crudete from being saved:</h2>

      <ul>
      <% crudete.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
